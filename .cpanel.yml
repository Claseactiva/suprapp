---
# Prueba minima: validar que cPanel ejecute tareas de deploy en esta cuenta
deployment:
  tasks:
    - export DEPLOYPATH=/home/supra/suprapp_deploy_test/
    - /bin/mkdir -p $DEPLOYPATH
    - /usr/bin/touch $DEPLOYPATH/deploy_probe.txt
